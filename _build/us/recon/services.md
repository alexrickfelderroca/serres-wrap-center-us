# SERRES V12 — `services/` inventory (read-only recon)

Root: `C:\Users\Rickfelder\Desktop\Serres web\Serres wrap center webpage\Serres wrap center V12\`
Files: `services\{ppf,vinyl,ceramic,detailing,paint-correction,body-kits}.html` (989 / 1312 / 668 / 664 / 753 / 699 lines)

---

## 0. CRITICAL FINDING — there is NO price table or price card on ANY of the six service pages

Verified by grep for `€`, `"price"`, and every plausible class name (`pricing|price|tier|pack|plan|card`) across all six files: **zero visible price markup**. Prices appear on the service pages in only four places, all prose/metadata:

1. `<meta name="description">` + `og:description` + `twitter:description`
2. `<p class="lead">` in the hero (one sentence, "desde N €")
3. FAQ answer #1 (always "¿Cuánto cuesta …?") as plain prose
4. `application/ld+json` `Service.offers[]` (or `hasOfferCatalog` on body-kits)

**The tier/price-card component lives only in `pages\prices.html`.** Full markup pattern in §8 below. `index.html` has no price markup either (only a nav link `pages/prices.html` at `index.html:699`).

---

## 1. Shared architecture (identical across all six)

- **No shared stylesheet.** Each page carries its ENTIRE CSS inline in `<style>` at line 30 → (page-specific end). Only two external CSS files: `/assets/fonts.css` (line 29, before `<style>`) and `/assets/serres-logo.css` (last line of `<head>`, loaded last so it overrides inline `.logo`).
- **`:root` token block is byte-identical on all six** (verified):
  ```
  --bg:#0a0a0b;--bg-2:#0e0e10;--panel:#141417;--panel-2:#191920;
  --line:rgba(255,255,255,0.09);--line-strong:rgba(255,255,255,0.16);
  --text:#f3f3f5;--muted:#9a9aa3;--muted-2:#6e6e77;
  --chrome:linear-gradient(176deg,#fdfdfe 0%,#cfcfd6 32%,#8d8d97 52%,#f0f0f4 72%,#a6a6b0 100%);
  --ease:cubic-bezier(.22,.61,.36,1);--maxw:1280px;
  ```
- **Fonts:** Barlow Condensed (display/UI, self-hosted) + DM Sans (body). Preloaded woff2 at lines 27–28, declared in `/assets/fonts.css` (generated by `_build/fetch-fonts.mjs`). Fallback stack everywhere: `"Barlow Condensed","Bahnschrift","Arial Narrow",sans-serif`.
- **`<html lang="es">`** on all six. No i18n runtime is loaded on service pages — `assets/serres-i18n.js` is NOT referenced; only `window.SERRES_I18N` *optional* lookups inside the ppf/vinyl carousel JS (`function T(s){return (window.SERRES_I18N&&window.SERRES_I18N.t)?window.SERRES_I18N.t(s):s;}` — `ppf.html:874`, `vinyl.html:915`). `data-i18n-skip` attributes are present on brand names / JS-populated containers.
- **Head order (identical):** charset, viewport, `<title>`, description, canonical, og:type/site_name/locale/title/description/url/image(+w/h), twitter:card/title/description/image, 4 favicons, `theme-color #0a0a0b`, 2 font preloads, fonts.css, `<style>`, JSON-LD blocks, gtag (`G-1K6FYZ99GN`), serres-logo.css.
- **Tail scripts (identical):** reveal-on-scroll IntersectionObserver; before/after comparison slider (5 of 6); `<script src="../assets/serres-enhance.js" defer>`; a gtag click-tracker delegating `wa.me` → `whatsapp_click` and `tel:` → `phone_click`.
- **`assets/serres-enhance.js` (358 lines)** injects the mobile hamburger menu, floating WhatsApp button and stat count-up on every page. Hardcoded constants at lines 14–20: `WA_DIGITS="34649663380"`, `IG_URL="https://www.instagram.com/serres.wrap.center/"`, `TEL_TEXT="+34 649 66 33 80"`. Menu array lines 30–38 (`Services / Projects / Exclusive / Prices / Why SERRES / Blog / Contact`). **This file is a site-wide retarget point.**

### Shared CSS classes every service page relies on (all defined inline per page, not shared)
`.wrap` (maxw 1280 / 40px gutter, 22px ≤760) · `.display` · `.chrome-text` · `.eyebrow` (+`::before` rule) · `.btn` / `.btn.ghost` / `.btn .arr` (the 2-variant button system; clip-path notched corner) · `header.nav` / `.nav-inner` / `.nav-left` / `.nav-right` / `.logo` / `.back` (+`span.lbl`) · `.crumbs` (+`.c-sep`/`.sep`/`.c-here`/`[aria-current]` — **markup varies per page, see §7**) · `.hero` / `.hero-top` / `.hero-overlay` / `.hero-grain` / `.hero-poster` / `.lead` / `.hero-stats` / `.hstat b` / `.hstat span` / `.h1-kw` / `.ln` · `.scroll-cue` (+`.bar`) · `.section-head` / `.section-head h2` / `.sh-note` · `.xform` / `.xform-grid` / `.xtext` / `.xspecs .row .k .v` · `.compare` / `.compare .after` / `.compare .before` / `.cmp-tag.bef` / `.cmp-tag.aft` / `.cmp-divider` / `.cmp-handle` / `.cmp-hint` / `.compare.touched` · `.process` / `.proc-grid` / `.stage` / `.st-no` / `.st-tool` · `.faq` / `.faq-list` / `.faq-item` (+ **5 different disclosure-icon variants**, §6) · `.cta` / `.cta .actions` / `.cta .phone` · `footer` · `.reveal-up` / `.reveal-up.in` · `@keyframes rise|fade|slide` · media queries at 980px / 760px / `prefers-reduced-motion:reduce`.

---

## 2. `services/ppf.html` (989 lines)

**Head**
- title (L6): `PPF en Barcelona — Protección de Pintura | SERRES`
- description (L7) = og:description (L13) = twitter:description (L20): `Instalación de PPF autorreparable con más de 50 colores de varias marcas profesionales. Packs frontal y coche completo desde 890 €. Sant Cugat, Barcelona.`
- canonical (L8) / og:url (L14): `https://serreswrapcenter.es/services/ppf.html`
- og:image (L15) / twitter:image (L21): `https://serreswrapcenter.es/assets/og/ppf.jpg` (1200×630)
- inline `<style>` L30–298; JSON-LD `Service` L299–371; `FAQPage` L372–427; `BreadcrumbList` L428–438; gtag L439–446; serres-logo.css L447; `</head>` L448

**H1** (L476–479): `<h1 class="display">` → line 1 `PPF` (`.chrome-text`, `data-i18n-skip`), line 2 `<span class="h1-kw">PPF en Barcelona — protección de pintura para tu coche</span>`

**Sections**
| Lines | Section | Heading |
|---|---|---|
| 451–461 | `header.nav` | — |
| 464–489 | `.hero` — **no video/img**; background is a CSS radial glow `.hero::after` | H1 above; eyebrow `Servicio 03 · Paint Protection Film`; crumbs inline at L466–472 (inside `.wrap`); hero-stats L481–485 = `50+ Colores de film` / `100% Reversible` / `3 Capas de film`; lead L487 |
| 492–526 | `.xform` | H2 `Entra en brillo.<br>Sale en satinado.` (L497) |
| 529–582 | `.protect` | H2 `Protección que<br>no se ve.` (L534) |
| 585–617 | `.colors` | H2 `Protégelo.<br>Exprésalo.` (L590) |
| 620–693 | `.faq#faq` (inline `<style>` L621–645) | H2 `PPF, en <span class="chrome-text">claro.</span>` (L650) |
| 696–706 | `.cta` | H2 `Protege lo que <span class="chrome-text">conduces.</span>` (L699) |
| 708 | `footer` | `© 2026 SERRES … Paint Protection Film · Inozetek · 3M` |

**Prices (JSON-LD only, L326–369)** — `Service.offers[]`, `@type Offer` with nested `priceSpecification` `valueAddedTaxIncluded:true` + `availability InStock` + `url`:
- `PPF frontal` — **890 EUR**
- `PPF frontal completo` — **1190 EUR**
- `PPF carrocería completa` — **2390 EUR**
Also in prose: lead L487 ("desde 890 €"), FAQ #1 L658, FAQ #6 L683 (cross-ref "Ceramic Coating SiO₂ desde 340 €").

**Gallery / image blocks**
- Before/after slider L512–522: `.compare.touched#compare` → `img.after` `../assets/ppf/after.webp` (1000×1500) + `img.before#beforeImg` `../assets/ppf/before.webp` (1290×1710), `.cmp-tag.bef`("Antes / Brillo de fábrica"), `.cmp-tag.aft`("Después / PPF satinado"), `.cmp-divider#divider`, `button.cmp-handle#handle`, `.cmp-hint`. Driver JS at L783+ region; slider JS mirrors ceramic's (auto-demo sequence `[50,66,34,50]`).
- `.xspecs` L504–509: Vehículo=`BMW M2` / Film=`PPF satinado autorreparable` / Cobertura=`Cobertura de carrocería completa` / Garantía=`3 años`
- `.feat-grid` L538–559 — 4 × `.feat` (`.fnum` 01–04): `Defensa contra impactos`, `Capa superior autorreparable`, `Resistente a manchas y UV`, `Totalmente reversible`
- `.construct` L561–580 — `.ct-copy h3` `Tres capas,<br>una piel invisible.` + `.stack` with 3 × `.layer.l1/.l2/.l3`, each `<span class="ly-name">` + `<span class="ly-micron">`
- **Color coverflow** L595–615 (markup) + L783–968 (data+logic). Empty containers `#colBrands`, `#colFilters`, `#cfTrack`, `#cfIndex`, `#cfTotal`, `#cfPrev`, `#cfNext`, `#cfRail`. **63 color entries** across 2 brands: `{id:"inozetek",label:"Inozetek"}` L787, `{id:"3m",label:"3M"}` L832. Entry shape `{n:"Piano Black",c:"DPPF701",f:"Gloss",h:"#0b0b0d"}` (+`h2` for Color Shift). `FX` finish map at L862. Card template L898–903:
  ```html
  <article class="film-card" data-finish="{FX}" data-i="{i}">
    <div class="film-skin" style="--c:{hex|gradient}"><span class="film-flag">Color Shift</span></div>
    <div class="film-meta"><span class="film-name">{n}</span><span class="film-code">{brand} · {c}</span></div>
    <div class="film-fam">{f}</div>
  </article>
  ```
  Filter chip template L884 / L892: `<button class="chip[ active]" data-b|data-f="…">{label}<span class="ct">{count}</span></button>`
- `.col-note` L614 — disclaimer prose.

**FAQ** — `<details class="faq-item">` × 6, icon `<span class="fq-x"></span>` (CSS-drawn plus/cross, L632–636), answer is a bare `<p>` (no class). **FAQPage JSON-LD: YES**, L372–427, 6 Q&A, text identical to visible prose.
Q1 `¿Cuánto cuesta instalar PPF en Barcelona?` L657 · Q2 `¿Cuánto dura el PPF y qué garantía tiene?` L662 · Q3 `¿El PPF se autorregenera de verdad?` L667 · Q4 `¿Cómo es el proceso de instalación?` L672 · Q5 `¿Cuántos días tarda y cómo pido cita?` L677 · Q6 `¿Qué es mejor, PPF o tratamiento cerámico?` L681

**CTA blocks** — nav CTA L458 (`.btn` → `../index.html#contact`); section `.cta` L696–706: eyebrow `Blinda tu coche`, `.actions` with `.btn`→`../index.html#contact` and `.btn.ghost`→`../pages/gallery.html`, `.phone` `Sant Cugat del Vallès, Barcelona · <strong>+34 649 66 33 80</strong>`.
**Related links** `.rel-links` L687–691 → `vinyl.html`, `ceramic.html`.

**Page-only CSS classes:** `.protect .feat-grid .feat .fnum .construct .ct-copy .stack .layer .l1 .l2 .l3 .ly-name .ly-micron .colors .col-filters .chip .ct .coverflow .cf-stage .cf-track .film-card .film-skin .film-fam .film-flag .film-meta .film-name .film-code .cf-controls .cf-count .arrows .arrow .cf-rail .col-note .fq-x .rel-links .rel-label`

---

## 3. `services/vinyl.html` (1312 lines) — the largest page

**Head**
- title (L6): `Car Wrap en Barcelona — Cambio de Color | SERRES`
- description (L7/13/20): `Car Wrap: cambio de color con films 3M, Avery Dennison e Inozetek. Más de 150 colores. Coche completo desde 1.490 €. Sant Cugat del Vallès.`
- canonical L8 / og:url L14: `…/services/vinyl.html`; og:image `…/assets/og/vinyl.jpg`
- `<style>` L30–331; JSON-LD **BreadcrumbList first** L332–342; `Service` L343–413; `FAQPage` L414–469; gtag L470–477; logo css L478; `</head>` L479

**H1** (L504–508): `Car` / `Wrap`(`.chrome-text`) / `<span class="h1-kw">Car Wrapping en Barcelona — vinilar tu coche</span>`

**Sections**
| Lines | Section | Heading |
|---|---|---|
| 482–492 | `header.nav` | — |
| 494–496 | `nav.crumbs.wrap` — **outside** the hero, between header and hero | — |
| 499–518 | `.hero` — background is a **JS canvas** (`.hero-film-canvas` injected at L1266, texture `../assets/vinyl/wrap-texture.webp` L1107) + `.hero-film-veil`; `.hero.film-on` toggles opacity | eyebrow `Servicio 01 · Car Wrap`; hero-stats `150+ Colores de film` / `3 Marcas profesionales` / `100% Reversible`; lead L516 |
| 521–554 | `.palette` | H2 `Todos los colores para<br>vinilar tu coche` (L526) |
| 557–597 | `.xform` | H2 `Un panel a<br>la vez.` (L566 — **inside `.xtext`, not `.section-head`**) |
| 600–615 | standalone `<style>` for FAQ | — |
| 617–659 | `.faq#faq` | H2 `Antes de vinilar<br>tu coche` (L622) |
| 662–672 | `.cta` | H2 `¿Has encontrado tu <span class="chrome-text">color?</span>` (L665) |
| 674 | `footer` | `… Car Wrap · 3M · Avery Dennison · Inozetek` |

**Prices (JSON-LD only, L371–411)** — `Service.offers[]`, `priceSpecification` carries `valueAddedTaxIncluded:true` **and `minPrice`**:
- `Acentos en vinilo (techo, retrovisores, pilares)` — **250 EUR**
- `Cambio de color completo` — **1490 EUR**
- `Cambio de color Signature (desmontaje ampliado)` — **1990 EUR**
`Service.provider.@type` = **`AutoRepair`** (differs from ppf/ceramic which use `LocalBusiness`).
Prose: lead L516; FAQ #1 L630.

**Gallery / image blocks**
- **Palette scroller** L531–552: `#brandFilters`, `#filters` (`.filters`+`.chip`), `.scroll-tools`/`.scroll-hint`/`#prev`/`#next`, `.pal-viewport#viewport[data-at-start]` → `.pal-track#track`, `.pal-progress > i#palFill`. **190 colors across 3 brands**: `3m` L691 (60, series `Wrap Film Serie 2080`), `avery` L757 (71, `Supreme Wrapping Film`), `inozetek` L838 (59, `Super Gloss`); array closes L904. `FX` map L907–910. Swatch template L929–941:
  ```html
  <article class="swatch" data-finish="${FX[col.f]}">
    <div class="sw-chip" style="--c:${chipBg(col)}"><span class="flip-flag">Flip</span></div>
    <div class="sw-meta">
      <div class="sw-name">${col.n}</div>
      <div class="sw-sub"><span class="sw-code">${brand}${' · '+col.c}</span><span class="sw-fin">${col.f}</span></div>
    </div>
  </article>
  ```
- Before/after slider L583–593: `../assets/vinyl/xm-after.webp` (1400×933) / `xm-before.webp` (1248×936); tags `Brillo de fábrica` → `Car Wrap satinado SERRES`
- `.xspecs` L569–574: `BMW XM` / `3M™ 2080 Satin Black` / `Cambio de color de carrocería completa` / `5–7 días`
- `.rel-links` L575–579 (inside `.xtext`, `.rel-k` label): → `ppf.html`, `ceramic.html`
- **Dead CSS:** `.wrap-ripple / .wr-overlay / .wr-hint` defined L134–156 but **no matching markup** — leftover.

**FAQ** — 6 × `<details class="faq-item">`, icon `<span class="fq-ico">＋</span>` (fullwidth plus, rotated 45° when open; circular 34px bordered button), answer `<p class="fq-a">`. **FAQPage JSON-LD: YES**, L414–469, 6 Q&A.
Q1 `¿Cuánto cuesta vinilar un coche completo?` L629 · Q2 `¿Cuántos días tarda un cambio de color completo?` L634 · Q3 `¿Cuánto dura el vinilo y qué garantía tiene?` L639 · Q4 `¿El vinilo daña la pintura original?` L644 · Q5 `¿Cómo es el proceso, desde el presupuesto hasta la entrega?` L649 · Q6 `¿Cómo pido cita y dónde estáis?` L654

**CTA** — nav L489; `.cta` L662–672 (same two buttons + `.phone`).

**Page-only CSS classes:** `.hero-film-canvas .hero-film-veil .film-on .wrap-ripple .wr-overlay .wr-hint .palette .pal-brands .pal-controls .filters .chip .ct .scroll-tools .scroll-hint .hbar .arrows .arrow .pal-viewport .pal-track .swatch .sw-chip .flip-flag .sw-meta .sw-name .sw-sub .sw-code .sw-fin .pal-progress .fq-ico .rel-links .rel-k`

---

## 4. `services/ceramic.html` (668 lines)

**Head**
- title (L6): `Tratamiento Cerámico para Coche en Barcelona | SERRES`
- description (L7/13/20): `Tratamiento Ceramic Coating SiO2 con hasta 5 años de protección. Preparación y pulido según pack. Desde 340 €. Sant Cugat, Barcelona.`
- canonical L8 / og:url L14: `…/services/ceramic.html`; og:image `…/assets/og/ceramic.jpg`
- `<style>` L30–238; `Service` L239–289; `FAQPage` L290–345; `BreadcrumbList` L346–356 (breadcrumb item 2 uses `…/index.html#services`, unlike ppf/vinyl which use `…/#services`); gtag L357–364; logo css L365; `</head>` L366

**H1** (L397–401): `Ceramic` / `Coating`(`.chrome-text`) / `<span class="h1-kw">Tratamiento cerámico para coche en Barcelona</span>`

**Sections**
| Lines | Section | Heading |
|---|---|---|
| 369–379 | `header.nav` | — |
| 382–412 | `.hero` (`min-height:90vh/90svh`) — `<video autoplay muted loop playsinline preload="metadata" poster="../assets/ceramic/poster-opt.jpg">` + `<source src="../assets/ceramic/ceramic.mp4">` L383–385; `.hero-overlay` L386; `.hero-grain` L387; `nav.crumbs` L388–392 (absolute, inside hero, wraps its own `.wrap`) | eyebrow `Servicio 02 · Ceramic Coating`; hero-stats `SiO₂ Recubrimiento` / `5años Protección` / `2 Capas`; lead L409; `.scroll-cue` L411 |
| 415–449 | `.xform` | H2 `El agua<br>se suelta.` (L420) |
| 452–483 | `.process` | H2 `Prepara. Aplica.<br>Cura.` (L457) |
| 486–507 | standalone `<style>` (FAQ + rel-links) | — |
| 509–557 | `.faq#faq` | H2 `Antes de reservar.` (L514) |
| 560–570 | `.cta` | H2 `Fija el <span class="chrome-text">brillo.</span>` (L563) |
| 572 | `footer` | `… Ceramic Coating SiO₂` |

**Prices (JSON-LD only, L265–287)** — `Service.offers[]`, **simplest form: no `priceSpecification`, no `availability`**, each `Offer` has `name`/`price`/`priceCurrency`/`description`:
- `Cerámico Essential` — **340 EUR** — "Pulido de una etapa y una capa de Ceramic Coating SiO2. Durabilidad estimada de 2 años."
- `Cerámico Signature` — **590 EUR** — "Corrección de dos etapas, doble capa cerámica y tratamiento antilluvia en cristales. 3 años."
- `Cerámico Concours` — **890 EUR** — "Corrección completa, capas cerámicas adicionales y protección interior. 5 años."
Prose: FAQ #1 L522, FAQ #2 L527, FAQ #3 L532.

**Gallery / image blocks**
- Before/after slider L435–445: `../assets/ceramic/after.webp` / `before.webp` (both 600×896); tags `El agua queda plana` → `Forma gotas y resbala`. **`.compare` here is `aspect-ratio:3/4`** (L153).
- `.xspecs` L427–432: `Sedán · Negro brillo` / `Cerámica SiO₂ · 2 capas` / `Hidrófobo · autolimpiante` / `Hasta 5 años`
- `.proc-grid` L461–480 — 3 × `.stage` (`.st-no` 01/02/03, `h3`, `.st-tool`, `p`): `Preparación y descontaminación` (`Lavado · clay · corrección de pintura`), `Aplicación` (`Cerámica SiO₂ · nivelada a mano`), `Curado` (`Curado controlado · 24h de reposo`)
- No carousel, no `duo`.

**FAQ** — 6 × `<details class="faq-item">`, icon `<span class="fq-ic">+</span>`, answer `<p class="fq-a">`. **FAQPage JSON-LD: YES**, L290–345, 6 Q&A, verbatim match.
Q1 `¿Cuánto cuesta un tratamiento cerámico en SERRES?` L521 · Q2 `¿Qué incluye cada pack cerámico?` L526 · Q3 `¿Cuánto dura el tratamiento en el coche?` L531 · Q4 `¿Cuántos días necesita el coche en el taller?` L536 · Q5 `¿El cerámico elimina los arañazos existentes?` L541 · Q6 `¿Cómo reservo cita?` L546

**CTA** — nav L376; `.cta` L560–570 (eyebrow `Sella el brillo`).
**Related** `.rel-links` L551–555, `.rel-label` + `a.rel-link` → `paint-correction.html`, `ppf.html`.

**Page-only CSS classes:** `.process .proc-grid .stage .st-no .st-tool .scroll-cue .bar .fq-ic .rel-links .rel-label .rel-link`

---

## 5. `services/detailing.html` (664 lines) — no comparison slider

**Head**
- title (L6): `Detailing y Limpieza Interior de Coche en Barcelona | SERRES`
- description (L7/13/20): `Limpieza integral: vapor, tapicería, cuero y motor. Deep Clean desde 150 €, Showroom Reset desde 490 €. Estudio premium en Sant Cugat.`
- canonical L8 / og:url L14: `…/services/detailing.html`; og:image `…/assets/og/detailing.jpg`
- `<style>` L30–285; **only `BreadcrumbList` in `<head>`** (L286–292, minified one-line style); gtag L293–300; logo css L301; `</head>` L302
- **`FAQPage` JSON-LD is at L515–570 and `Service` JSON-LD at L571–646 — i.e. in the BODY tail, after `serres-enhance.js`.** Unique to this page.

**H1** (L334–338): `Como recién` / `entregado`(`.chrome-text`) / `<span class="ln ln-kw"><span class="h1-kw">Detailing de coche en Barcelona</span></span>`

**Sections**
| Lines | Section | Heading |
|---|---|---|
| 305–315 | `header.nav` | — |
| 317–321 | `nav.crumbs` — **outside** hero, static (not absolute), wraps `.wrap` | — |
| 324–349 | `.hero` — `<video … poster="../assets/detailing/poster-opt.jpg">` + `../assets/detailing/detailing.mp4` L325–327; overlay/grain L328–329 | eyebrow `Servicio 05 · Detailing`; hero-stats `2 Dentro y fuera` / `100% Acabado a mano` / `0 Atajos`; lead L346; `.scroll-cue` L348 |
| 352–372 | `.xform` (interior) | H2 `De usado<br>a como nuevo.` (L357) + `.sh-note` L359 |
| 375–395 | `.xform` (exterior, `style="padding-top:64px"`) | H2 `De lleno de polvo<br>a brillo profundo.` (L380) + `.sh-note` L382 |
| 398–436 | `.process` | H2 `Retira. Limpia.<br>Protege.` (L403) |
| 439–483 | `.faq#faq` (CSS inside main style block, L249–262) | H2 `Antes de reservar.` (L444) |
| 486–496 | `.cta` | H2 `Haz que se sienta <span class="chrome-text">nuevo otra vez.</span>` (L489) |
| 498 | `footer` | `… Detailing de interior y exterior` |

**Prices (JSON-LD only, L598–644)** — `Service.offers[]` with `priceSpecification` (`valueAddedTaxIncluded:true` + `minPrice`) + `availability` + `url`. `Service.provider.@type` = **`AutoDetailing`**:
- `Detailing Refresh` — **35 EUR**
- `Detailing Deep Clean — limpieza interior a vapor` — **150 EUR**
- `Detailing Showroom Reset — interior y exterior completo` — **490 EUR**
Prose: lead L346 ("Deep Clean desde 150 €"), FAQ #1 L453, FAQ #3 L463.

**Gallery / image blocks — the `.duo` pattern (unique to this page, reusable)**
```html
<div class="duo reveal-up">
  <div class="duo-cell bef">
    <img src="../assets/detailing/int-before.webp" alt="…" width="1290" height="2300" loading="lazy" decoding="async" draggable="false">
    <div class="duo-tag"><small>Antes</small>Polvo y suciedad</div>
  </div>
  <div class="duo-cell aft">
    <img src="../assets/detailing/int-after.webp" alt="…" width="1290" height="2256" loading="lazy" decoding="async" draggable="false">
    <div class="duo-tag"><small>Después</small>Limpio a vapor</div>
  </div>
</div>
```
Two instances: interior L361–370 (`int-before.webp` 1290×2300 / `int-after.webp` 1290×2256, tags `Polvo y suciedad` → `Limpio a vapor`); exterior L384–393 (`ext-before.webp` 1290×1997 / `ext-after.webp` 1290×1693, tags `Sucio y apagado` → `Brillo profundo`).
`.proc-grid` L408–427 — 3 × `.stage`: `Descontaminar` (`Lavado con espuma · clay · descontaminación`), `Limpieza profunda` (`Vapor · extracción · cepillos`), `Tratar y proteger` (`Sellador · acondicionador · UV`).
`.rel-links` L429–434 (`.rl-label` + `a` + `.rl-dot`) → `ppf.html`, `ceramic.html`.
**Note:** despite having no `.compare` markup, `.compare/.cmp-*` CSS and the leftover `.gbar .gb-fill` reduced-motion rule are still present in the stylesheet.

**FAQ** — 6 × `<details class="faq-item">`, icon `<span class="fq-ico">+</span>`, answer `<p class="fq-a">`. **FAQPage JSON-LD: YES** but at L515–570 (body tail), 6 Q&A.
Q1 `¿Cuánto cuesta un detailing completo?` L452 · Q2 `¿Cuánto tiempo tarda el servicio?` L457 · Q3 `¿Qué incluye la limpieza interior a vapor?` L462 · Q4 `¿Elimináis manchas de tapicería y cuero?` L467 · Q5 `¿El resultado está garantizado? ¿Cómo lo controláis?` L472 (contains trust claims `4,9` rating and `98%` recommend) · Q6 `¿Cómo reservo cita y dónde estáis?` L477

**CTA** — nav L312; `.cta` L486–496 (eyebrow `Por dentro y por fuera`).

**Page-only CSS classes:** `.duo .duo-cell .bef .aft .duo-tag .process .proc-grid .stage .st-no .st-tool .scroll-cue .fq-ico .rel-links .rl-label .rl-dot`

---

## 6. `services/paint-correction.html` (753 lines)

**Head**
- title (L6): `Pulido y Corrección de Pintura de Coche en Barcelona | SERRES`
- description (L7/13/20): `Pulido por etapas a máquina: Etapa 1, 2 o 3 según el estado de tu pintura. Adiós a arañazos, remolinos y hologramas. Sant Cugat, Barcelona.` — **the only page whose meta description contains no price.**
- canonical L8 / og:url L14: `…/services/paint-correction.html`; og:image `…/assets/og/paint-correction.jpg`
- `<style>` L30–292; `BreadcrumbList` L293–303 (item 3 name `Pulido de coche`); `Service` L304–379; `FAQPage` L380–435; gtag L436–443; logo css L444; `</head>` L445

**H1** (L474–478): `Pulido` / `por etapas`(`.chrome-text`) / `<span class="h1-kw">Pulido de coche en Barcelona — corrección de pintura</span>`

**Sections**
| Lines | Section | Heading |
|---|---|---|
| 448–458 | `header.nav` | — |
| 461–489 | `.hero` — `<video … poster="../assets/correction/poster-opt.jpg">` + `../assets/correction/correction.mp4` L462–464; overlay/grain L465–466; `nav.crumbs` L467–469 (absolute, **no separator spans — `/` injected via `.crumbs a::after`**) | eyebrow `Servicio 04 · Corrección de pintura`; hero-stats `3 Pulido por etapas` / `95% Eliminación de defectos` / `0 Hologramas`; lead L486; `.scroll-cue` L488 |
| 492–526 | `.xform` | H2 `De turbio<br>a espejo.` (L497) |
| 529–583 | `.process` (includes `.gloss` meter) | H2 `Etapa 1. Etapa 2.<br>Etapa 3.` (L534) + `.sh-note` L536 |
| 586–626 | `.faq#faq` (CSS inside main style block, L263–284) | H2 `Antes de reservar<br>tu corrección` (L591) |
| 629–639 | `.cta` | H2 `Devuelve el <span class="chrome-text">brillo.</span>` (L632) |
| 641 | `footer` | `… Pulido por etapas a máquina` |

**Prices (JSON-LD only, L337–377)** — `Service.offers[]`, `priceSpecification` with `valueAddedTaxIncluded:true` (no `minPrice`) + `availability`. `Service.provider.@type` = **`AutoBodyShop`**, and provider carries an **`aggregateRating` (4.9 / 50 reviews / best 5)** at L326–331:
- `Corrección + Ceramic Coating Essential` — **340 EUR**
- `Corrección + Ceramic Coating Signature` — **590 EUR**
- `Corrección + Ceramic Coating Concours` — **890 EUR**
Prose: FAQ #1 L598, FAQ #5 L614 (cross-ref "PPF … desde 890 € el frontal").

**Gallery / image blocks**
- Before/after slider L512–522: `../assets/correction/after.webp` / `before.webp` (both 1086×1448); tags `Micro-arañazos y turbidez` → `Brillo corregido`
- `.xspecs` L504–509: `BMW Coupé · Negro brillo` / `Pulido por etapas · Etapa 3` / `Micro-arañazos · hologramas · oxidación` / `Sellado y protegido`
- `.proc-grid` L539–558 — 3 × `.stage`: `Etapa 1 · Corte` (`Boina de corte · compound agresivo`), `Etapa 2 · Refinado` (`Boina de pulido · pulido medio`), `Etapa 3 · Acabado` (`Boina de acabado · pulido fino`)
- **`.gloss` GU meter L560–581 (unique, reusable data-viz block):**
  ```html
  <div class="gloss reveal-up" id="glossSection">
    <div class="gl-copy"><h3>Los números,<br>no solo el brillo.</h3><p>…</p></div>
    <div class="gl-meter">
      <div class="gbar before">
        <div class="gb-top"><span class="gb-label">A la llegada</span>
          <span class="gb-val">42<small style="font-size:14px;color:var(--muted-2);letter-spacing:.1em"> GU</small></span></div>
        <div class="gb-track"><div class="gb-fill" data-w="44"></div></div>
      </div>
      <div class="gbar after">
        <div class="gb-top"><span class="gb-label">Tras la corrección</span>
          <span class="gb-val chrome-text">94<small style="font-size:14px;letter-spacing:.1em"> GU</small></span></div>
        <div class="gb-track"><div class="gb-fill" data-w="96"></div></div>
      </div>
    </div>
  </div>
  ```
  Values: **42 GU → 94 GU**; bar widths `data-w="44"` / `data-w="96"`; animated by JS at L655–671.

**FAQ** — 6 × `<details class="faq-item">`; **first one has the `open` attribute (L596)**; **no icon span at all** — the `+`/`×` is `.faq-item summary::after` (L272–274); answer `<p class="faq-a">` (note: `faq-a`, not `fq-a`). **FAQPage JSON-LD: YES**, L380–435, 6 Q&A.
Q1 `¿Cuánto cuesta un pulido de coche en Barcelona?` L597 · Q2 `¿Cuánto tiempo tarda la corrección de pintura?` L601 · Q3 `¿El pulido elimina todos los arañazos?` L605 · Q4 `¿Cómo es el proceso y cómo sé que el resultado es real?` L609 · Q5 `¿El resultado tiene garantía? ¿Cuánto dura?` L613 · Q6 `¿Cómo reservo cita y necesito dejar el coche todo el día?` L617

**CTA** — nav L455; `.cta` L629–639 (eyebrow `Recupera la profundidad`).
**Related** `.faq-links` L621–624 (styled arrow links, unique class): → `ceramic.html`, `ppf.html`.

**Page-only CSS classes:** `.gloss .gl-copy .gl-meter .gbar .before .after .gb-top .gb-label .gb-val .gb-track .gb-fill .faq-a .faq-links .process .proc-grid .stage .scroll-cue`

---

## 7. `services/body-kits.html` (699 lines)

**Head**
- title (L6): `Montaje de Body Kits en Barcelona | SERRES`
- description (L7/13/20): `Instalación y pintura de body kits, spoilers y widebody con ajuste OEM. Desde 450 €. Sant Cugat del Vallès.`
- canonical L8 / og:url L14: `…/services/body-kits.html`; og:image `…/assets/og/body-kits.jpg`
- `<style>` L30–265; `Service` L266–341; `FAQPage` L342–397; `BreadcrumbList` L398–408 (item 2 uses `…/index.html#services`); gtag L409–416; logo css L417; `</head>` L418

**H1** (L445–449): `Hecho para` / `impactar`(`.chrome-text`) / `<span class="ln ln-kw"><span class="h1-kw">Montaje de body kits en Barcelona</span></span>`

**Sections**
| Lines | Section | Heading |
|---|---|---|
| 421–431 | `header.nav` | — |
| 434–460 | `.hero` — **static image hero, not video**: `<img class="hero-poster" src="../assets/svc/bodykit.webp" width="1200" height="896" loading="eager" fetchpriority="high" decoding="async" alt="…">` L435; overlay/grain L436–437; `nav.crumbs` L439–441 **inside `.wrap`** with `<span class="sep">/</span>` | eyebrow `Servicio 06 · Body Kits`; hero-stats `Todos Tipos de kit` / `OEM Encaje` / `360° Transformación`; lead L457; `.scroll-cue` L459 |
| 463–498 | `.xform` | H2 `De serie a<br>arma de calle.` (L468) |
| 501–531 | `.process` (repurposed as "What we fit") | H2 `Kits, llantas<br>y colas.` (L506) |
| 534–588 | `.faq#faq` (CSS inside main style block, L207–224) | H2 `Antes de montar<br>tu body kit.` (L539) |
| 591–601 | `.cta` | H2 `Dale presencia <span class="chrome-text">de verdad.</span>` (L594) |
| 603 | `footer` | `… Body Kits · Llantas a medida · Colas de escape` |

**Prices (JSON-LD only, L298–339)** — **the only page using `hasOfferCatalog`/`OfferCatalog` instead of a flat `offers[]` array**. `priceSpecification` uses `minPrice` (no `price` key inside the spec) + `valueAddedTaxIncluded:true`. `Service.provider.@type` = **`AutoBodyShop`** with `aggregateRating` 4.9 / 50 / 5 (L287–292). `OfferCatalog.name` = `"Tarifas de montaje de body kits"`:
- `Complementos aerodinámicos (splitter, difusor, spoiler)` — **450 EUR**
- `Kit completo con ajuste y pintura` — **1490 EUR**
- `Transformación integral / widebody` — **3490 EUR**
Prose: lead L457 ("desde 450 €"), FAQ #1 L548.

**Gallery / image blocks**
- Before/after slider L484–494: `../assets/bodykit/after.webp` / `before.webp` (both 600×896); tags `Trasera de serie` → `Kit aero completo`
- `.xspecs` L475–480: `VW Golf GTI Mk7 · Tornado Red` / `Difusor trasero · alerón · splitters` / `Colas de escape en negro brillo` / `Atornillado · nivel OEM`
- `<p class="xlinks">` L481 (inline related links inside `.xtext`): → `vinyl.html`, `ppf.html`
- `.proc-grid` L510–529 — 3 × `.stage`: `Body Kits` (`Todos los tipos · OEM y aftermarket`), `Llantas a medida` (`Medidas · acabados · encaje`), `Colas de escape` (`Negro brillo · cromo · carbono`)

**FAQ — structurally different from all others.** 6 × `<details class="faq-item">`; icon `<span class="faq-ico"></span>` (empty, CSS-drawn 34px circle with a `+` made from two pseudo-elements, L216–221); **answer is wrapped in `<div class="faq-body"><p>…</p></div>`**, not a classed `<p>`. **FAQPage JSON-LD: YES**, L342–397, 6 Q&A.
Q1 `¿Cuánto cuesta montar un body kit?` L546 · Q2 `¿Cuánto tarda la instalación?` L553 · Q3 `¿El montaje tiene garantía?` L560 · Q4 `¿Cómo es el proceso de montaje?` L567 · Q5 `¿Puedo traer mi propio kit o lo pedís vosotros?` L574 · Q6 `¿Cómo pido cita y dónde estáis?` L581

**CTA** — nav L428; `.cta` L591–601 (eyebrow `Cambia la postura`).

**Page-only CSS classes:** `.xlinks .faq-ico .faq-body .process .proc-grid .stage .scroll-cue`

---

## 8. The price/tier card pattern — verbatim reusable spec (source: `pages\prices.html`)

**This is the ONLY tier component in the project.** If the US build needs price cards on service pages, lift this. Everything below is copy-exact.

### 8.1 CSS (`pages/prices.html:141–184`, plus responsive at 223–226)
```css
/* ---------- TIER CARDS ---------- */
.tiers-sec{padding:44px 0 10px}
.svc-intro{display:flex;align-items:baseline;justify-content:space-between;gap:24px;flex-wrap:wrap;margin-bottom:34px}
.svc-intro h2{font-family:"Barlow Condensed","Bahnschrift","Arial Narrow",sans-serif;font-weight:700;text-transform:uppercase;
  font-size:clamp(30px,4.4vw,54px);line-height:.94;letter-spacing:.012em}
.svc-intro p{color:var(--muted);font-size:15.5px;max-width:44ch}

.tier-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;align-items:stretch}
.tier{position:relative;display:flex;flex-direction:column;background:var(--panel);
  border:1px solid var(--line);padding:30px 28px 28px;
  clip-path:polygon(0 0,100% 0,100% 100%,18px 100%,0 calc(100% - 18px));
  transition:border-color .3s var(--ease),transform .3s var(--ease);}
.tier:hover{border-color:var(--line-strong);transform:translateY(-4px)}
.tier.popular{background:var(--panel-2);border:1px solid rgba(255,255,255,.34)}
.tier .pop-tag{position:absolute;top:18px;right:18px;font-family:"Barlow Condensed",…;
  font-weight:600;text-transform:uppercase;letter-spacing:.16em;font-size:10.5px;
  background:var(--chrome);color:#0a0a0b;padding:5px 12px;border-radius:999px}
.tier h3{font-family:"Barlow Condensed",…;font-weight:600;text-transform:uppercase;
  letter-spacing:.04em;font-size:24px;line-height:1.05}
.tier .t-desc{margin-top:8px;color:var(--muted);font-size:14px;line-height:1.5;min-height:42px}
.tier .t-price{margin-top:20px;display:flex;align-items:baseline;gap:8px}
.tier .t-from{font-family:"Barlow Condensed",…;text-transform:uppercase;
  letter-spacing:.2em;font-size:11px;color:var(--muted-2);white-space:nowrap;flex:none}
.tier .t-val{font-family:"Barlow Condensed",…;font-weight:700;font-size:46px;line-height:1;letter-spacing:.01em}
.tier .t-price.t-quote{flex-direction:column;align-items:flex-start;gap:6px}
.tier .t-quote-val{font-family:"Barlow Condensed",…;font-weight:700;font-size:34px;line-height:1;letter-spacing:.01em}
.tier .t-quote-note{font-family:"DM Sans",sans-serif;font-size:13px;line-height:1.45;color:var(--muted);text-transform:none;letter-spacing:0}
.tier ul{list-style:none;margin-top:22px;padding-top:18px;border-top:1px solid var(--line);
  display:flex;flex-direction:column;gap:9px;flex:1}
.tier li{display:flex;gap:11px;font-size:14px;color:var(--text);align-items:flex-start}
.tier li.off{color:var(--muted-2)}
.tier li .ic{flex:none;width:16px;height:16px;margin-top:3px}
.tier li .ic.ok{color:#e7e7ec}
.tier li .ic.no{color:var(--muted-2);opacity:.55}
.tier .t-cta{margin-top:24px}
.tier .t-cta .btn{width:100%;justify-content:center}
.tier .t-note{margin-top:12px;text-align:center;font-family:"Barlow Condensed",…;
  text-transform:uppercase;letter-spacing:.18em;font-size:10.5px;color:var(--muted-2)}

.price-note{margin-top:26px;display:flex;align-items:center;gap:14px;
  font-family:"Barlow Condensed",…;text-transform:uppercase;letter-spacing:.18em;
  font-size:11.5px;color:var(--muted-2);text-align:center;justify-content:center}
.price-note::before,.price-note::after{content:"";height:1px;background:var(--line);flex:1}

@media(max-width:980px){ .tier-grid{grid-template-columns:1fr;max-width:520px;margin:0 auto} .tier .t-desc{min-height:0} }
```

### 8.2 Card markup — exact shape (rendered form, `pages/prices.html:323`; template `:527–537`)
```html
<section class="tiers-sec" data-screen-label="Prices — Tiers">
 <div class="wrap">
  <div class="svc-intro" id="svcIntro">
    <h2>Car Wrap <span class="chrome-text">precios</span></h2>
    <p>{blurb}</p>
  </div>
  <div class="tier-grid" id="tierGrid" data-i18n-skip>

    <!-- ORDINARY TIER -->
    <article class="tier">
      <h3>Acentos</h3>
      <p class="t-desc">Techo, retrovisores y piezas de detalle — cambia la actitud, no el coche entero.</p>
      <div class="t-price">
        <span class="t-from">Desde</span>
        <span class="t-val chrome-text" data-price="250">250 €</span>
      </div>
      <ul>
        <li class=""><svg class="ic ok" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M4.5 12.5l5 5L19.5 7"></path></svg><span>Cobertura — Techo · retrovisores · acentos</span></li>
        <li class="off"><svg class="ic no" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6L6 18"></path></svg><span>Films premium y camaleón</span></li>
        <!-- exactly 6–7 <li>, one per comparison row -->
      </ul>
      <div class="t-cta">
        <a class="btn ghost" href="https://wa.me/34649663380?text=Hola%20SERRES%2C%20quer%C3%ADa%20presupuesto%20del%20pack%20Acentos%20de%20Car%20Wrap%20para%20mi%20coche." target="_blank" rel="noopener">Reserva por WhatsApp <span class="arr">→</span></a>
      </div>
      <p class="t-note">Desde 1 día en el taller</p>
    </article>

    <!-- HIGHLIGHTED (middle) TIER: adds .popular + .pop-tag, and its CTA drops .ghost -->
    <article class="tier popular">
      <span class="pop-tag">Más elegido</span>
      <h3>Cambio de color completo</h3>
      <p class="t-desc">…</p>
      <div class="t-price"><span class="t-from">Desde</span><span class="t-val chrome-text" data-price="1490">1.490 €</span></div>
      <ul>…</ul>
      <div class="t-cta"><a class="btn" href="https://wa.me/34649663380?text=…" target="_blank" rel="noopener">Reserva por WhatsApp <span class="arr">→</span></a></div>
      <p class="t-note">Desde 3–4 días en el taller</p>
    </article>

    <article class="tier">…</article>
  </div>
  <p class="price-note">Precios orientativos · el presupuesto final depende del tamaño del vehículo, su estado y la elección de film · no incluyen kit de mantenimiento</p>
 </div>
</section>
```

**"Quote instead of price" variant** (used when the service block sets `quote:true`, e.g. body kits) replaces the `.t-price` div with:
```html
<div class="t-price t-quote">
  <span class="t-quote-val chrome-text">On request</span>
  <span class="t-quote-note">Message us to calculate your price</span>
</div>
```
and the CTA label becomes `Request a quote`.

### 8.3 Icon constants (`pages/prices.html:468–469`)
```js
var OK='<svg class="ic ok" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M4.5 12.5l5 5L19.5 7"></path></svg>';
var NO='<svg class="ic no" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6L6 18"></path></svg>';
```

### 8.4 Rules an authoring agent must follow
- `data-price="<integer>"` drives the count-up animation (`animatePrice`, called at `:541–543`). Text content is the formatted value; **the ES build uses `1.490 €` (dot thousands, trailing €)** — USD needs `$1,490` and a new `fmtEur`→`fmtUsd`.
- Feature `<li>`s are **derived from the comparison table's rows** (`:520–525`): first 7 rows; `false` → `.off` + `NO` icon; `true` → `OK` icon + row label; a string value → `OK` icon + `"{row label} — {value}"`.
- The middle tier carries `pop:true` → `.popular` + `<span class="pop-tag">Más elegido</span>`, and its button is `.btn` (solid); the others are `.btn.ghost`.
- WhatsApp deep link: `https://wa.me/34649663380?text=` + `encodeURIComponent("Hola SERRES, quería presupuesto del pack "+tierName+" de "+serviceLabel+" para mi coche.")` (`:464`, `:526`). **Hardcoded ES phone — retarget point.**
- The same data also feeds a **comparison table** (`.cmp-sec`, `table.cmp`, CSS `:186–208`, render `:548–556`) with `thead`/`tbody`/`tfoot`; the `tfoot` row is `Guide price` → `from {price}`. `.col-pop` highlights the popular column.

### 8.5 Full pricing data model (`pages/prices.html:356–462`) — English strings, the port's source of truth
```
wrap      Car Wrap            250 / 1490* / 1990    Accents | Full Color Change | Signature Wrap
ppf       PPF                 890 / 1190* / 2390    Front Pack | Pro | Full Body
ceramic   Correction+Ceramic  340 / 590*  / 890     Essential | Signature | Concours
detailing Detailing            35 / 150*  / 490     Refresh | Deep Clean | Showroom Reset
bodykits  Body Kits (quote:true) 450 / 1490* / 3490 Aero Parts | Full Kit Fitted | Transformation
(* = pop:true)
```
Each also carries `rows:[…]` (6–8 comparison rows) and per-tier `note` strings (`'From 3–4 days in the studio'`, `'Approx. 3 hours'`, `'2–4 weeks · by consultation'`, …).

---

## 9. Cross-cutting retarget inventory (facts another agent will need)

| Item | Value | Where |
|---|---|---|
| Domain | `https://serreswrapcenter.es` | canonical, og:url, every JSON-LD `url`/`item`/`@id` on all 6 pages |
| Phone | `+34649663380` / display `+34 649 66 33 80` | JSON-LD `telephone`; `.cta .phone strong` on all 6; FAQ answers; `assets/serres-enhance.js:14,20` |
| Address | `Av. Can Fatjó dels Aurons, 15` (comma present on ppf/ceramic/paint-correction, absent on vinyl/detailing/body-kits) · `08174 Sant Cugat del Vallès, Barcelona, ES` | JSON-LD `PostalAddress` + FAQ prose on all 6 |
| `areaServed` | `City: Sant Cugat del Vallès` + `City: Barcelona` | all 6 |
| GA4 | `G-1K6FYZ99GN` | L358/471/439/294/437/410 |
| `provider.@type` | `LocalBusiness` (ppf, ceramic) · `AutoRepair` (vinyl) · `AutoDetailing` (detailing) · `AutoBodyShop` (paint-correction, body-kits) | — |
| `aggregateRating` | `4.9 / 50 / bestRating 5` — **only** on paint-correction (L326–331) and body-kits (L287–292) | — |
| Currency | `EUR`, `valueAddedTaxIncluded:true`, ES number format `1.490 €` | all Offers |
| Copyright | `© 2026 SERRES. Todos los derechos reservados.` | footer, all 6 |
| Internal links | `../index.html`, `../index.html#services`, `../index.html#contact`, `../pages/gallery.html`, sibling `*.html` | nav, CTA, rel-links |
| Images | 5 pages use `.webp` with `.jpg` twins present in `assets/<svc>/`; heroes: `ceramic.mp4`, `detailing.mp4`, `correction.mp4` (+ `poster-opt.jpg`), `assets/svc/bodykit.webp` (body-kits), none (ppf CSS glow, vinyl JS canvas) | `assets/{ppf,vinyl,ceramic,detailing,correction,bodykit,svc,og}/` |
| OG images | `assets/og/{ppf,vinyl,ceramic,detailing,paint-correction,body-kits}.jpg` — all exist | — |

**Existing port pipeline** (`C:\Users\Rickfelder\Desktop\serres miami\_build\`) already has `port/40-prices.js`, `port/30-geo-copy.js`, `port/32-seo-titles.js`, `port/38-jsonld-copy.js`, `port/65-nap-schema.js`, `port/59-us-english.js`, `port/60-seo-domain.js`, plus `port/geo-map.json`, `port/jsonld-map.json`, `port/miami.json` and recon notes including `port/recon/prices-currency.md` — i.e. the price/currency and NAP retargets are already modelled there and currently target "Miami".